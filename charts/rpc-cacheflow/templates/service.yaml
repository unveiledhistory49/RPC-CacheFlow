apiVersion: v1
kind: Service
metadata:
  name: {{ include "rpc-cacheflow.fullname" . }}
  labels:
    {{- include "rpc-cacheflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rpc-cacheflow.selectorLabels" . | nindent 4 }}
